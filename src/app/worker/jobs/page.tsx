'use client';

/**
 * 구직자 공고 검색 / Worker job search (placeholder)
 * 기존 /jobs 페이지에서 이관 — 비자 필터 포함
 */
export default function WorkerJobsPage() {
  return (
    <div className="max-w-6xl mx-auto px-4 py-6">
      <h1 className="text-xl font-bold text-gray-900 mb-6">공고 검색</h1>
      <div className="bg-white rounded-xl border border-gray-200 p-12 text-center">
        <p className="text-gray-400 text-sm">공고 검색 + 비자 필터 (기존 /jobs 이관 예정)</p>
      </div>
    </div>
  );
}
