services:
  frontend:
    build:
      context: .
      args:
        - NEXT_PUBLIC_API_URL=https://jobchaja.com
        - BACKEND_URL=http://job-backend:8000
    container_name: job-frontend
    environment:
      - BACKEND_URL=http://job-backend:8000
    networks:
      - default
      - backend-network

  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/certbot:/var/www/certbot:ro
    depends_on:
      - frontend
    networks:
      - default
      - backend-network

networks:
  default:
  backend-network:
    external: true
    name: job-platform-backend_default
